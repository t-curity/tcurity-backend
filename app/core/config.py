# app/core/config.py
"""
서비스 설정값 중앙 관리

모든 설정값은 이 파일에서 정의하고, 다른 파일에서 import하여 사용합니다.
"""

import os

# =====================================================
# 세션 설정
# =====================================================
SESSION_TTL_SECONDS = 10 * 60  # 10분 (세션 유효 시간)

# =====================================================
# Phase A 설정
# =====================================================
PHASE_A_TIME_LIMIT = 20  # 초 (FE 제한 시간)
PHASE_A_MAX_ATTEMPTS = 3  # 최대 재시도 횟수

# =====================================================
# Phase B 설정
# =====================================================
PHASE_B_TIME_LIMIT = 30  # 초 (문제 풀이 제한 시간)
PHASE_B_MAX_FAIL_COUNT = 3  # 최대 실패 횟수 (초과 시 BLOCKED)

# =====================================================
# AI 서버 설정
# =====================================================
AI_SERVER_URL = os.getenv("AI_SERVER_URL", "http://10.0.83.48:9000")

# =====================================================
# 클라이언트 인증 설정
# =====================================================
CREDENTIALS_FILE = os.path.join(
    os.path.dirname(os.path.dirname(os.path.dirname(__file__))),
    "client_credentials.json"
)